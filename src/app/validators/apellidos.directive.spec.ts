import { ApellidosDirective } from './apellidos.directive';

describe('ApellidosDirective', () => {
  it('should create an instance', () => {
    const directive = new ApellidosDirective();
    expect(directive).toBeTruthy();
  });
});
