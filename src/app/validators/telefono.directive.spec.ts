import { TelefonoDirective } from './telefono.directive';

describe('TelefonoDirective', () => {
  it('should create an instance', () => {
    const directive = new TelefonoDirective();
    expect(directive).toBeTruthy();
  });
});
